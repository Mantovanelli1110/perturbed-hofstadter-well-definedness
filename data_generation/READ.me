q_D_macro_profiles_stream_occ_win64.c
Generates the empirical data underlying the symbolic model.
It computes the Hofstadter-type sequence up to large bounds, extracts macro-block profiles, records debt transitions, and logs macro occurrences.
The resulting CSV files form the raw input for the symbolic reconstruction and constraint-solving pipeline.
